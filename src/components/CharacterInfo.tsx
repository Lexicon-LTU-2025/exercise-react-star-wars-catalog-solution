import type { ReactElement } from 'react';

export const CharacterInfo = (): ReactElement => {
  return (
    <section id="charcters-info">
      <header>
        <h2>Character Info</h2>
      </header>
    </section>
  );
};
